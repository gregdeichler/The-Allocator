<Page x:Class="TheAllocator.Pages.RestoreProgressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="*" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="30" FontWeight="Bold" Text="Restore Progress" />
            <TextBlock Margin="0,10,0,0"
                       FontSize="16"
                       Foreground="{StaticResource AllocatorMutedBrush}"
                       TextWrapping="Wrap"
                       Text="The app is restoring the backup into the selected profile, repairing access, and reconnecting the selected printers." />
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="StatusTitleText" FontSize="20" FontWeight="Bold" Text="Restore Progress" />
                <ProgressBar Grid.Row="1"
                             x:Name="RestoreProgressBar"
                             Margin="0,16,0,0"
                             Height="18"
                             Minimum="0"
                             Maximum="100"
                             IsIndeterminate="True"
                             Foreground="{StaticResource AllocatorBurgundyBrush}" />
                <ScrollViewer Grid.Row="3"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              CanContentScroll="True">
                    <TextBlock x:Name="ProgressText"
                               FontSize="15"
                               TextWrapping="Wrap" />
                </ScrollViewer>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    x:Name="FinishButton"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Finish_Click"
                    Content="Continue To Complete"
                    IsEnabled="False" />
        </Grid>
    </Grid>
</Page>
